{
  "_comment": "v11 grid — LightGBMClassifier proba thresholds. Values are absolute proba (0..1), not regression deltas. trailing_offset removed (v11 has framework trailing OFF, custom_stoploss owns the trail).",
  "strategy_file": "freqtrade/user_data/strategies/FinBuddyFreqAI.py",
  "backtest_script": "scripts/run_backtest.sh",
  "parse_script": "scripts/parse_backtest.py",
  "results_csv": "_autobacktest_results.csv",
  "model_cache_path": "freqtrade/user_data/models",
  "backtest_results_path": "freqtrade/user_data/backtest_results",
  "acceptance_criteria": {
    "win_rate": 50.0,
    "sharpe": 0.5,
    "max_drawdown": 20.0,
    "profit_factor": 1.2
  },
  "grid": {
    "stoploss": [-0.020, -0.025, -0.030],
    "ml_threshold": [0.50, 0.55, 0.60],
    "ml_exit_threshold": [0.40, 0.45, 0.50],
    "atr_threshold": [0.002, 0.003]
  },
  "_grid_rationale": {
    "stoploss": "Class attr; v11 keeps -0.020 to -0.030 sweep.",
    "ml_threshold": "Entry proba threshold for long+short. Bear branch auto-tightens to +0.05.",
    "ml_exit_threshold": "Exit proba threshold (opposite-class probability cut-off).",
    "atr_threshold": "ATR/close volatility floor."
  },
  "_removed_from_v3": {
    "trailing_offset": "v11 disables framework trailing — custom_stoploss handles it. Lever is dead.",
    "roi_multiplier": "Confirmed dead in v6 round 2."
  }
}
