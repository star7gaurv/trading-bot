# Shared Libraries

Reusable helpers used across the brain, the dashboard, and standalone scripts — the DRY layer
this project's own engineering principles require (see root `CLAUDE.md`).

::: lib.ft_creds

::: lib.live_price

::: lib.telegram_template

::: lib.pair_filter
